{
  "name": "skills",
  "description": "Routes web_search calls through a local library of Claude-style SKILL.md files, using a matching skill instead of a live search",
  "version": "0.1.0",
  "entry": "python3 skills.py",
  "runtime": "python3",
  "mode": "hook",
  "hook": "web_search",
  "toggle_prefix": "skills",
  "autostart": true,
  "permissions": ["filesystem"],
  "config": {
    "skills_dir": "~/.aulthium/skills",
    "min_score": "2"
  }
}
